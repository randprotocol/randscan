[package]
name = "randscan-indexer"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
randscan-core = { workspace = true }
randscan-db = { workspace = true }
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
reqwest = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
chrono = { workspace = true }
hex = { workspace = true }
# The receiver registry's verifier (spec 2026-09-17 §3): the explorer re-runs
# `ReceiverRecord::verify` rather than trusting the node. Path dependency into the fullnode
# checkout beside this repo (`../../../fullnode`), same pattern as randscan-viewing's version
# pinning against that workspace.
randprotocol-core = { path = "../../../fullnode/crates/randprotocol-core" }
